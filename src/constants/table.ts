/**
 * Shared table constants. Matches NetworkTable / AccountDetail table structure (Figma).
 */

/** Row height (px) for data tables: NetworkTable, TransactionsTable, PayoutsTable, InvoicesTable, ProductsTable, TableSkeleton. */
export const ROW_HEIGHT = 52
